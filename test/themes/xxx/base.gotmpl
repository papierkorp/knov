<!DOCTYPE html>
<html>
<head>
    <title>{{.Title}} - {{.CurrentMetadata.Name}}</title>
    <style>
        body {
            background-color: #1a1a1a;
            color: #e0e0e0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 20px;
        }
        .container {
            max-width: 800px;
            margin: 0 auto;
            background-color: #2d2d2d;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        }
        h1 {
            color: #4a9eff;
            border-bottom: 2px solid #4a9eff;
            padding-bottom: 10px;
        }
        .theme-info {
            background-color: #3d3d3d;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
            border-left: 4px solid #4a9eff;
        }
        .theme-selector {
            background-color: #3d3d3d;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 20px;
        }
        select {
            background-color: #4d4d4d;
            color: #e0e0e0;
            border: 1px solid #666;
            padding: 8px;
            border-radius: 4px;
        }
        button {
            background-color: #4a9eff;
            color: white;
            border: none;
            padding: 8px 16px;
            border-radius: 4px;
            cursor: pointer;
            margin-left: 10px;
        }
        button:hover {
            background-color: #357abd;
        }
        ul {
            background-color: #3d3d3d;
            padding: 20px;
            border-radius: 8px;
        }
        li {
            margin: 8px 0;
            padding: 8px;
            background-color: #4d4d4d;
            border-radius: 4px;
        }
        .metadata {
            font-size: 0.9em;
            color: #a0a0a0;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>{{.Heading}}</h1>
        
        <div class="theme-info">
            <h3>Current Theme: {{.CurrentMetadata.Name}}</h3>
            <div class="metadata">
                <p>Version: {{.CurrentMetadata.Version}} | Author: {{.CurrentMetadata.Author}}</p>
                <p>{{.CurrentMetadata.Description}}</p>
                {{if .CurrentMetadata.Features.DarkMode}}<p>✓ Dark Mode Enabled</p>{{end}}
            </div>
        </div>
        
        <div class="theme-selector">
            <h3>Theme Selector</h3>
            <form method="POST" action="/4">
                <select name="theme">
                    {{range .AvailableThemes}}
                    <option value="{{.}}" {{if eq . $.CurrentTheme}}selected{{end}}>{{.}}</option>
                    {{end}}
                </select>
                <button type="submit">Change Theme</button>
            </form>
        </div>

        <p>{{.Message}}</p>
        <p>Current time: {{.Time.Format "2006-01-02 15:04:05"}}</p>
        
        <h3>Items:</h3>
        <ul>
            {{range .Items}}
            <li>{{.}}</li>
            {{end}}
        </ul>
    </div>
</body>
</html>
