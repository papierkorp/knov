{{/* theme: builtin */}}
{{ define "content" }}
<div class="page-search">
    <h1>{{T "Search Results"}}</h1>
    <div class="search-form">
        <input
            type="text"
            placeholder="{{T "Search files..."}}"
            hx-get="/api/search?format=cards"
            hx-target="#page-search-results"
            hx-trigger="keyup changed delay:300ms, load, change from:#search-titleonly, change from:#search-history"
            hx-include="#search-titleonly, #search-history"
            name="q"
            id="search-page-input"
            value="{{ .Data.SearchQuery }}"
        />
        <div class="search-options">
            <label class="search-option-label">
                <input type="checkbox" id="search-titleonly" name="titleonly" value="true"/>
                <span>{{T "title only"}}</span>
            </label>
            <label class="search-option-label">
                <input type="checkbox" id="search-history" name="history" value="true"/>
                <span>{{T "search history"}}</span>
            </label>
        </div>
    </div>
    <div id="page-search-results">
        {{ if .Data.SearchQuery }}
            <p>{{T "Loading results..."}}</p>
        {{ else }}
            <p>{{T "Enter a search term"}}</p>
        {{ end }}
    </div>
</div>
{{ end }}
